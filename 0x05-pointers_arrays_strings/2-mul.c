#include "main.h"
#include <stdio.h>

/**
* mul - function that multiplies two integers
* @a: First integer
* @b: Second integer
* Return: mul.
*/
int mul(int u, int v
{
	int x;
	x = u * v;
return (x);
}
