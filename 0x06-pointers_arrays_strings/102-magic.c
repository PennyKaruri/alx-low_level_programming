#include "main.h"

* - only one statement
   * - you are not allowed to code anything else than this line of code
   */
   
int main (void)

  *(p+5) = 98;
  *(p+5) = 98;
  /* ...so that this prints 98\n */
  printf("a[2] = %d\n", a[2]);
  return 0;

